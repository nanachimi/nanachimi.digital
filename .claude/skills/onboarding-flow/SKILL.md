I want to improve conversion on nanachimi.digital by testing different messaging approaches and refining the onboarding experience.

Use the available skills when relevant.

Tasks:

1. Create 3 Hero variations for A/B testing (in German):
   - Each must follow problem → solution → outcome structure
   - Avoid technical terms like "App" or "Software"
   - Avoid "Unternehmen" and "Geschäft"
   - Use simple language (Idee, Vorhaben, Aufgaben, Alltag)
   - Each variation must include:
     - headline (problem-focused)
     - subheadline (solution + credibility + end-to-end service)
     - CTA (can include “Go-Live in 48h möglich”)

2. Define a simple A/B testing strategy:
   - how to structure Hero variants in the codebase
   - how to assign variants (simple randomization)
   - what metrics to track (CTR, onboarding start, conversion to call, conversion to direct start)

3. Refactor the onboarding flow:
   - adapt it to non-technical, early-stage users
   - simplify wording and remove technical language
   - ensure each step communicates value clearly
   - include reassurance (no technical knowledge required, we handle everything end-to-end)

4. Implement dual conversion paths after estimate:
   - option 1: book a call
   - option 2: start directly (no call)
   - design clear UX for both paths

5. Improve onboarding entry on homepage:
   - simple explanation
   - non-technical language
   - clear value proposition

6. Ensure consistency:
   - messaging in Hero and onboarding must align
   - tone must be simple, clear, and trust-oriented (German market)

Important:
- All outputs must be in German
- Focus on UX, structure, and strategy (no full code yet)
- Keep everything simple and realistic for V1